# Manager: Architect PR マージ後の Setup Issue 設計ワークフロー

## ゴール

- Architect の docs PR マージ後に、
  - 初期 Setup 方針を簡潔に決める。
  - **単一の Setup Issue** に、具体的な編集指示と Acceptance Criteria をきっちり書く。
  - その Setup Issue を入口として Engineer に実装させる準備をする。

---

## 手順 1: 状況の確認と使用技術の選定

1. 直前にレビューし、ユーザーにマージしてもらった Architect PR（例: `#15`）を前提とする。
2. `docs/REQUIREMENT.md` や関連ドキュメント（`docs/USE_CASES.md`, `docs/DOMAIN_ER.md` など）を参照し、
   - プロジェクトで使用する技術スタック（言語・フレームワーク・テスト・Lint/Format・CI 方針）を決める。
   - docs にすでに方針が書かれている場合は、それに従う。
   - 曖昧な場合は、Manager が一貫性のあるスタックを選定し、Setup Issue 内で明示する。

---

## 手順 2: 単一の Setup Issue を作成する

1. Setup 用 Issue テンプレート（`setup.md`）を使い、次の 2 点を中心に簡潔かつ具体的に記述する：
   - **目的と作業内容**
     - 何をセットアップするか（技術スタック・Makefile・テスト・Lint/Format・CI・ROADMAP など）。
     - 特に **Roadmap（`ROADMAP.md` など docs 系）は構成や項目をできるだけ具体的に指定する**。
     - ツール設定（Makefile / CI / Lint/Format / テスト設定）は、使用ツール名と実行コマンド方針を明示する。
   - **Acceptance Criteria**
     - 下記のようなチェックリストを、Engineer が迷わず判断できるレベルで書く：
       - `make setup` / `make test` / `make lint` / `make format` の成否
       - CI でどのコマンドが Green になっているべきか
       - ROADMAP.md を初期化する場合、どのアウトライン・内容になっていればよいか

2. Acceptance Criteria は、Engineer が「要件の解釈」で迷わないよう、できるだけ厳密に箇条書きで定義する：
   - 典型的には次のような項目を含める：
     - `make setup` がローカルで成功する（定義する場合）
     - `make test` がローカルで成功する
     - `make lint` がローカルで成功する
     - `make format` がローカルで成功する（定義する場合）
     - GitHub Actions の CI で `make test` / `make lint` が成功している
     - Issue の「作業内容」に書かれた編集指示がすべて反映されている
     - ROADMAP.md を初期化する場合、この Issue に書かれたアウトラインと内容が一致している

3. 上記を踏まえて、次のようなコマンドを想定する（実際の実行はユーザーまたは別フロー）：

   ```sh
   gh issue create \
     --template setup.md \
     --title "[SETUP] プロジェクト初期セットアップと ROADMAP 初期化" \
     --label setup
   ```

   - `setup.md` テンプレート内の各セクション（目的 / 変更対象ファイル / 作業内容 / Acceptance Criteria）に、ここで決めた内容を具体的に書く。

---

## 手順 3: Engineer 呼び出し用アクションブロックを表示

1. 作成した Setup Issue の番号を確認する（例: `#10`）。
2. ユーザーに、Setup Issue から実装を開始してもらうよう、次のアクションブロックを表示して終わる：

   ```md
   ::: action 🛑 User Action Required
   **Status:** Architect PR マージ後の Setup Issue 作成完了
   **Next Step:** 新しいチャットを開き、Engineer ロールを呼び出してください。
   **Next Prompt:**
   > @ai-engineer.mdc /engineer-setup Setup 用 Issue #<setup_issue_number> から実装を開始してください。  
   > （この Issue には Makefile / CI / Lint / Format / ROADMAP.md 初期化などの編集方針が書かれています）
   :::
   ```